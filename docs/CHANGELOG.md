# Docs Changelog

## Unreleased
- removed the obsolete `etl` legacy entry point (`etl/__init__.py`, `etl/legacy.py`) to avoid confusing it with the production services; the historical Keepa example now lives in `docs/legacy_samples/keepa_etl_example.py` as documentation-only code
