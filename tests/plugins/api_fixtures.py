"""API-specific pytest plugin hook module.

Currently a placeholder to satisfy pytest's requirement that plugins live at the
repository root. Add API-wide fixtures here when they need to be shared beyond
`services/api/tests`.
"""

# Intentionally empty: fixtures remain local to API tests where appropriate.
